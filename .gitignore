# Runtime state (alert timestamps) — machine-specific, don't commit
state.json

# Never commit real secrets. The SMTP password comes from the
# FX_SMTP_PASSWORD environment variable / GitHub secret, not from config.json.
config.local.json
.env

__pycache__/
*.pyc
*.log
.DS_Store
